package MODEL;

/**
 * Enum that represents the type of the image on slots with the same colour.
 */
public enum Type {
    TYPE1,              //First type
    TYPE2,              //Second type
    TYPE3,              //Third type
}
